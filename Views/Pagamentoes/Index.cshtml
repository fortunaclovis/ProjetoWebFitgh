@model IEnumerable<WebFight.Models.Pagamento>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Bolsa_Id)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Valor)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.MultaMora)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.JurosMora)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DataVencimento)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DataPagamento)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Bolsa_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MultaMora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JurosMora)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataVencimento)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DataPagamento)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Pagamento">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Pagamento">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Pagamento">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
